import React from 'react';

const Header = () => {
  return (
    <header style={{ padding: '1rem', background: '#222', color: '#fff' }}>
      <h1>SnapToSketch</h1>
    </header>
  );
};

export default Header;
